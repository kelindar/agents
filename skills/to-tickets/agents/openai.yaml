interface:
  display_name: "To Tickets"
  short_description: "Publish a spec with minimal issue clutter"
  default_prompt: "Use $to-tickets to publish the approved spec and plan as the smallest useful ticket hierarchy."
policy:
  allow_implicit_invocation: false
